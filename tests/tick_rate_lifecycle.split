state ["hundred.exe", "hundred-twenty.exe"] {}

tickRate {
    attached: 60,
    detached: 2,
}

onAttach {
    if process.name() == "hundred.exe" {
        let requestedRate: u16 = 100
        setTickRate(requestedRate)
    } else {
        setTickRate(120.0 as f32)
    }
}
