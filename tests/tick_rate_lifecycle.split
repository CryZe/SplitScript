state ["hundred.exe", "hundred-twenty.exe"] {}

setup {
    setTickRate(60)
}

onDetach {
    setTickRate(60)
}

onAttach {
    if process.name() == "hundred.exe" {
        setTickRate(100)
    } else {
        setTickRate(120)
    }
}
