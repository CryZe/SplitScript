state ["hundred.exe", "hundred-twenty.exe"] {}

tickRate {
    attached: 60,
    detached: 2,
}

onAttach {
    if process.name() == "hundred.exe" {
        setTickRate(100)
    } else {
        setTickRate(120)
    }
}
